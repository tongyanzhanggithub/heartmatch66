# 【当前：HTTP 明文过渡方案】Caddy 监听 80，反代到 app:3001，浏览器访问 http://服务器公网IP
# ⚠️ 明文传输：嘉宾填的身份证后四位/微信/照片在网络上不加密，仅适合临时测试，
#    请勿长期对外收真实报名。尽快切换到下方的 HTTPS 方案。
:80 {
    encode gzip
    reverse_proxy app:3001
}

# 【以后上 HTTPS】拿到域名（含免费 DuckDNS 子域名）后，把上面整段换成下面这样、改成你的域名，
# 然后 `docker compose restart caddy` 即可自动签发受信任证书，无需改 docker-compose：
# your-domain.com {
#     encode gzip
#     reverse_proxy app:3001
# }
