# 依赖与构建产物
node_modules/
client/dist/
mobile/dist/

# 数据库与备份（含嘉宾隐私数据，严禁上传）
server/data.db
server/data.db-*
server/backups/
server/data/

# 环境变量（含密钥与密码，严禁上传）
.env
.env.production
*.local

# 部署包与临时文件
*.zip
prd_text.txt
